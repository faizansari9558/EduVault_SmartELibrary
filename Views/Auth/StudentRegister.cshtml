@model SmartELibrary.ViewModels.StudentControlledRegistrationViewModel
@{
    ViewData["Title"] = "Student Registration";
}

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
        <div class="card auth-card">
            <div class="card-body">
                <h4 class="mb-3">Student Registration</h4>
                <p class="text-muted small">Complete registration using Enrollment Number and Date of Birth from admin uploaded data.</p>

                <div asp-validation-summary="All" class="text-danger mb-2"></div>

                <form asp-action="VerifyStudentRegistration" method="post" class="mb-3">
                    @Html.AntiForgeryToken()
                    <div class="mb-2">
                        <label asp-for="EnrollmentNo" class="form-label"></label>
                        <input asp-for="EnrollmentNo" class="form-control" readonly="@(Model.IsVerified ? "readonly" : null)" />
                    </div>
                    <div class="mb-2">
                        <label asp-for="DateOfBirth" class="form-label"></label>
                        <input asp-for="DateOfBirth" type="date" class="form-control" readonly="@(Model.IsVerified ? "readonly" : null)" />
                    </div>

                    @if (!Model.IsVerified)
                    {
                        <button type="submit" class="btn btn-outline-primary w-100">Verify Details</button>
                    }
                </form>

                @if (Model.IsVerified)
                {
                    <form asp-action="CompleteStudentRegistration" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="EnrollmentNo" />
                        <input type="hidden" asp-for="DateOfBirth" />
                        <input type="hidden" asp-for="IsVerified" />

                        <div class="mb-2">
                            <label asp-for="Name" class="form-label"></label>
                            <input asp-for="Name" class="form-control" readonly />
                        </div>
                        <div class="mb-2">
                            <label asp-for="Password" class="form-label"></label>
                            <input asp-for="Password" type="password" class="form-control" />
                        </div>
                        <div class="mb-3">
                            <label asp-for="ConfirmPassword" class="form-label"></label>
                            <input asp-for="ConfirmPassword" type="password" class="form-control" />
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Create Account</button>
                    </form>
                }

                <div class="mt-3 text-center">
                    <a href="/Login" class="link-btn-primary">Back to Login</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
